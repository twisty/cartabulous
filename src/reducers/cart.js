/**
 * Define an initial state.
 */
const initialState = {
  addedIds: [],
  quantityById: {}
}

/**
 * Just a stub noop reducer.
 */
export default function cart(state = initialState, action) {
    return state;
}
